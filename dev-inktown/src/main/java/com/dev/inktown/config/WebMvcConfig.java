/*
package com.dev.inktown.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

*/
/*
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**") // Map all paths
                .allowedOrigins("http://localhost:5173") // Allow the specific origin you need
                .allowedMethods("GET", "POST", "OPTIONS", "PUT", "DELETE") // Allow all common HTTP methods
                .allowedHeaders("*"); // Allow all headers (or specify which ones needed)
//                .allowCredentials(true); // Allow sending cookies (if applicable)
    }
*//*

}
*/
